"use strict";

/** @jsx createElement */
const visit = require('unist-util-visit');

const _require = require('./helpers'),
      createElement = _require.createElement,
      replace = _require.replace;

module.exports = ({
  markdownAST
}, options = {}) => {
  visit(markdownAST, 'image', node => {
    if (!node.alt) return;
    replace(node).with(createElement("unknown", {
      tagName: "figure",
      className: options.figureClassName
    }, createElement("image", Object.assign({}, node, {
      alt: null,
      className: options.imageClassName
    })), createElement("paragraph", {
      tagName: "figcaption",
      className: options.captionClassName
    }, node.alt)));
  });
};
//# sourceMappingURL=index.js.map